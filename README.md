# WireMock.NET UI

Современный веб-интерфейс для управления WireMock.NET сервером, построенный на Blazor Server и MudBlazor.

## Возможности

- 📊 **Dashboard** - Обзор состояния WireMock сервера
- 🗺️ **Управление Mappings** - Просмотр, создание, редактирование и удаление mock-маппингов
- 📡 **Просмотр Requests** - Мониторинг всех входящих запросов
- 🎨 **Modern UI** - Красивый интерфейс на основе Material Design (MudBlazor)
- ⚡ **Real-time Updates** - Blazor Server для интерактивного взаимодействия

## Требования

- .NET 9.0 SDK
- WireMock.NET сервер (запущенный локально или в Docker)

## Быстрый старт

### 1. Запуск WireMock сервера

Самый простой способ - использовать Docker:

```bash
docker run -d \
  --name wiremock \
  -p 9091:8080 \
  sheyenrath/wiremock.net-standalone:latest
```

Или используйте предоставленный docker-compose:

```bash
docker-compose up -d
```

### 2. Запуск UI приложения

```bash
cd WireMockUI
dotnet restore
dotnet run
```

Приложение будет доступно по адресу: `https://localhost:5001` или `http://localhost:5000`

## Конфигурация

Настройте URL WireMock сервера в `appsettings.json`:

```json
{
  "WireMock": {
    "BaseUrl": "http://localhost:9091"
  }
}
```

## Структура проекта

```
WireMockUI/
├── Components/
│   ├── Layout/          # Компоненты разметки (MainLayout, NavMenu)
│   ├── Pages/           # Страницы приложения (Home, Mappings, Requests)
│   ├── App.razor        # Главный компонент приложения
│   └── Routes.razor     # Конфигурация маршрутизации
├── Models/              # Модели данных для WireMock API
├── Services/            # Сервисы для работы с WireMock API
├── wwwroot/            # Статические файлы
├── Program.cs          # Точка входа приложения
└── appsettings.json    # Конфигурация приложения
```

## Использование

### Просмотр Mappings

1. Перейдите на страницу **Mappings**
2. Просмотрите все существующие маппинги
3. Создавайте новые маппинги с помощью кнопки "Create Mapping"
4. Удаляйте ненужные маппинги
5. Используйте "Reset All" для очистки всех маппингов

### Просмотр Requests

1. Перейдите на страницу **Requests**
2. Просматривайте все входящие запросы к WireMock серверу
3. Анализируйте детали запросов (метод, URL, время, IP)
4. Очищайте историю запросов кнопкой "Clear All"

## API WireMock.NET

Приложение использует следующие эндпоинты WireMock API:

- `GET /__admin/mappings` - Получение всех маппингов
- `POST /__admin/mappings` - Создание маппинга
- `PUT /__admin/mappings/{id}` - Обновление маппинга
- `DELETE /__admin/mappings/{id}` - Удаление маппинга
- `DELETE /__admin/mappings` - Удаление всех маппингов
- `GET /__admin/requests` - Получение всех запросов
- `DELETE /__admin/requests` - Удаление всех запросов

## Технологии

- **.NET 9.0** - Последняя версия .NET
- **Blazor Server** - Для создания интерактивного веб-интерфейса
- **MudBlazor** - Библиотека UI компонентов Material Design
- **WireMock.NET** - HTTP mock сервер

## Дальнейшие улучшения

- [ ] Диалоговое окно для создания/редактирования маппингов
- [ ] Детальный просмотр запросов и маппингов
- [ ] Экспорт/импорт маппингов
- [ ] Поиск и фильтрация
- [ ] Поддержка сценариев (Scenarios)
- [ ] Валидация запросов
- [ ] Темная тема
- [ ] Аутентификация

## Лицензия

MIT

## Вклад

Pull requests приветствуются! Для серьёзных изменений, пожалуйста, сначала откройте issue для обсуждения.
